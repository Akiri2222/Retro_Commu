<%= form_with model: @post, local: true do |f| %>
  <h4>タイトル</h4>
    <%= f.text_field :title %>
  <h4>投稿ジャンル</h4>
    <%= f.collection_select(:genre_id, Genre.all, :id, :name) %>
  <h4>概要</h4>
    <%= f.text_area :text %>
  <%= f.submit "投稿" %>
<% end %>