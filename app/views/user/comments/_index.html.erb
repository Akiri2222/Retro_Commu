 コメント件数：<%= @post.comments.count %>

<div class="container">
  <div class="row">
    <div class="col">
      <% @post.comments.each do |comment| %>
          <table class="table table-borderless">
            <tr>
              <td class="col-1"><%= comment.user.account_name %></td>
              <td><%= comment.comment %></td>
              <td>
                <% if comment.user==current_user %>
                  <%= link_to "編集", edit_post_comment_path(post.id,comment.id),class: 'btn btn-primary' %>
                  <%= link_to "削除", post_comment_path(post.id,comment.id),class: 'btn btn-dark',method: :delete, remote: true %>
                <% end %>
              </td>
            </tr>
          </table>
      <% end %>
    </div>
  </div>
</div>