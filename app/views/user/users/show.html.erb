<%= @user.account_name %>のマイページ
<table>
  <% if current_user.id == @user.id %>
  <tr>
    <td>名前</td>
    <td><%= @user.name %></td>
  </tr>
  <% end %>
  <tr>
    <td>アカウント名</td>
    <td><%= @user.account_name %></td>
  </tr>
  <tr>
    <td>メールアドレス</td>
    <td><%= @user.email %></td>
  </tr>
  <tr>
    <td>自己紹介</td>
    <td><%= @user.biography %></td>
  </tr>
  <tr>
    <%= link_to "いいねした投稿", book_marks_user_path(@user) %>
  </tr>
</table>

<%= link_to '情報編集', edit_user_path %>